import {RangeChart} from './chart.js';

let NAna = new RangeChart();

export const NA = {
    'NA': {
        'NA': {
            'name': 'Select a position!',
            'range': NAna
        }
    }
};